不能有中文路径或中文名
否则会出现问题
具体导致的原因，还没有跟踪到，希望设置这样一种断点，在一个变量的值改变的时候，才会触发断点


现在从csv改成excel
我的很多csv的都要修改为excel
这个可以接受，问题是假如下次再修改类型
难道再改吗？


在配置多个excel文件的前提下
是否会出现问题？？


excel和csv的默认编码格式都不是utf-8
那么他们的默认“编码”格式是什么？
我这个程序读取的时候，会不会读取的是乱码？
这个雷还是要解决的！
（暂时只支持utf8的编码）


mongo中的_id是系统维护的
自己可以手动创建吗？


如果配置了多个文件要导入
那么，可能会存在冲突
因为读取是异步读取的！
所以，会产生问题

如果collection存在
就清理一下库
这个还没有做！！

1、excel中的字段类型最好改变一下
_id为文本

excel中的列名不要有空格！！

单元格内的任何地方，尤其是前面和后面不要出现tab键！
通过导入到csv中就可以看到是否有tab键！

使用注意：
表名和路径最好都是在英文下!!
好像不是因为中文名的问题，而是一直在绝对路径前加了相对路径。。。

在这里应该把我的处理思路写下来

_id尽量使用string类型！

这个程序应该和powerDesigner进行配合
powerDesigner设计好表之后，导出excel的表结构供我使用
表结构里包含了字段的元数据信息，因为excel里的信息可能不是很准确！

表名和字段名都是大写英文字母

读取的信息包含2部分
一部分是从powerDesigner中导出的表结构信息
一部分是从用户那里得到的excel数据！
